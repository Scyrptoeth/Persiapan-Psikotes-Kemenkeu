# Plan

1. Create Next.js project configuration and local project instructions.
   - Verify: `npm install`, `npm run typecheck`.
2. Implement deterministic question generation and scoring.
   - Verify: unit tests for counts, bounds, decimal precision, and scoring.
3. Add Supabase schema migration and seed scripts.
   - Verify: migration/seed run against production project.
4. Implement auth helpers, session registration, and single-session status API.
   - Verify: typecheck and route-level validation.
5. Build protected dashboard, practice, history, settings, and admin screens.
   - Verify: build and browser render.
6. Create project skills `/start-persiapan-psikotes-kemenkeu` and `/update-persiapan-psikotes-kemenkeu`.
   - Verify: SKILL.md metadata and references exist.
7. Configure GitHub and Vercel deployment.
   - Verify: push succeeds and Vercel deployment is live.

